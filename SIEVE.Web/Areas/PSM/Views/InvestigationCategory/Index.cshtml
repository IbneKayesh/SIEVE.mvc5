@model  SIEVE.Web.Areas.PSM.Models.PSM_INVES_CATEGORY_VM
@{
    ViewBag.Title = "Investigation Category";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card card-primary card-outline">
    <div class="card-header">
        <h3 class="card-title">Find your Investigation Category</h3>
        <div class="card-tools">
            <a href="@Url.Action("Create","InvestigationCategory", new { Area ="PSM", id=""})" class="btn btn-outline-primary btn-flat btn-sm"><i class="fas fa-plus-circle"></i> Add @ViewBag.Title</a>
        </div>
    </div>
    <!-- /.card-body -->
    <div class="card-body">
        @using (Html.BeginForm())
            {
            @Html.ValidationSummary(false, "", new { @class = "text-danger" })
            @Html.AntiForgeryToken()
            <div class="row">
                <div class="col-md-3 col-sm-12">
                    @Html.LabelFor(model => model.CAT_NAME, htmlAttributes: new { @class = "control-label" })<i class="text-danger">*</i>
                    @Html.EditorFor(model => model.CAT_NAME, new { htmlAttributes = new { @class = "form-control form-control-sm", @Placeholder = Html.DisplayNameFor(x => x.CAT_NAME) } })
                    @Html.ValidationMessageFor(model => model.CAT_NAME, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="row">
                <div class="col">
                    <div class="form-group float-right mt-3">
                        <button type="submit" class="btn btn-sm btn-flat btn-primary"><i class="fa fa-search"> Search</i></button>
                    </div>
                </div>
            </div>
        }
    </div>
    <!-- /.card-body -->
    <div class="card-body">
        @if (Model.PSM_INVES_CATEGORY != null && Model.PSM_INVES_CATEGORY.Count > 0)
        {
            <div class="h-auto d-inline-block table-responsive">
                <table id="example1" class="table table-sm table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Category</th>
                            <th>#</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.PSM_INVES_CATEGORY)
                        {
                            <tr>
                                <td>@item.CAT_ID</td>
                                <td>@item.CAT_NAME</td>
                                <td class="text-right">
                                    <div class="dropdown">
                                        <button class="btn btn-secondary btn-sm dropdown-toggle" type="button" id="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                            Actions
                                        </button>
                                        <div class="dropdown-menu" aria-labelledby="#">
                                            <a href="@Url.Action("Create","InvestigationCategory", new { Area ="PSM", id=@item.CAT_ID})" class="dropdown-item btn btn-outline-danger btn-flat btn-sm"><i class="fas fa-edit"></i> Edit</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
